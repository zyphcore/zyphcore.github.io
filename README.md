# 3DS File Uploader to Discord

A simple Node.js server that allows file uploads via a webpage (works with Nintendo 3DS browser too) and sends them to a Discord webhook.

## Setup

1. Edit `config.json` with your Discord webhook.
2. Run:

```bash
npm install
node server.js
```

3. Go to `http://localhost:3000` and upload your file.
